<Project>

  <!-- Common settings for every project in the repository. -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Deterministic>true</Deterministic>

    <!-- Single source of the build version. Keep in lockstep with addon/config.yaml on every release;
         the `status` tool reports it so agents can verify which build prod is running. -->
    <Version>0.52.0</Version>
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>

    <!-- Code analysis (built-in CA quality + IDE style rules). Warnings stay visible locally;
         CI runs `dotnet build -warnaserror` to keep main zero-warning. -->
    <EnableNETAnalyzers>true</EnableNETAnalyzers>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
  </PropertyGroup>

</Project>
